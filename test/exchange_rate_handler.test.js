'use strict';

var handler = require('../lib/exchange_rate_handler');

describe('ExchangeRateHander', function() {
	describe('#work()', function() {
		context('when success', function() {
			it('should release the job', function(done) {
				// TODO
//				handler().work();
				done();
			});
		});
	});
});